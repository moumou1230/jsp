<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
<mapper namespace="co.yedam.mapper.BoardMapper">
	<select id="boardList" resultType = "co.yedam.vo.BoardVO">
		select *
		from tbl_board
		order by board_no
	</select>
	<select id = "boardListpaging" parameterType="int" resultType="co.yedam.vo.BoardVO">
		select b.*
		from(select rownum rn, a.*
		     from  (select *
		            from tbl_board
		            order by board_no) a
		     <![CDATA[
		     where rownum <= (#{page} * 5)) b 
		     ]]><!-- 비교연산자 떄문에 이코드에 써줘야됨 -->
		where b.rn > (#{page} - 1) *5
	</select>
	
	<select id = "getTotalCnt"  resultType="int">
		select count(1)
		from tbl_board
	</select>
	
	<select id="selectBoard" parameterType="int" resultType="co.yedam.vo.BoardVO">
		select board_no,
		       title,
		       content,
		       writer,
		       click_cnt,
		       creation_date
		from tbl_board
		where board_no = #{baordNo}
	</select>
	
	<insert id="insertBoard" parameterType="co.yedam.vo.BoardVO">
		insert into tbl_board(board_no, title, content, writer)
		values (board_seq.nextval, #{title}, #{content}, #{writer})
	</insert>
	
	<update id="updateBoard" parameterType="co.yedam.vo.BoardVO">
		update tbl_board 
		set title=decode(#{title},null,title,#{title}),
		    content=decode(#{content},null,content,#{content}) 
		where board_no = #{baordNo}
	</update>
</mapper>